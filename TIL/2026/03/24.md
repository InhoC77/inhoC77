----13---

공개키/비밀키

암호화 -> 키가 있어야함

대칭 암호 / 비대칭 암호

대칭암호
-> 암호화 할때랑 복호화할때의 키가 똑같음(대칭키)

비대칭 암호
-> 암호화 할때 키 != 복호화할때의 키(비대칭 키)
-> 키가 두개임
-> A / B (하나는 암호화용, 하나는 복호화용)
-> 공개키 / 비밀키

ssh

client		--------->		ssh
비밀키					공개키

ssh -i(identify) [비밀키가 포함된 파일명] [들어갈곳주소 ex)bandit14@localhost]

더 이상 overthewire에서 localhost에서 들어가는게 불가능해짐
권한 이상 있으면 바꿔줘야함

scp -> ssh 연결을 통해 파일을 복제하는 명령어
scp -P(포트번호) [포트번호] [유저@ssh비밀키위치 ex)bandit.labs.overthewire.org:/home/bandit13/ssh.private] [내 컴퓨터에 복사할 경로]






