bandit 하기위한 준비물
-> 리눅스 (vm or 듀얼부)
-> 리눅스 명령어 조금

ssh 접속방법

ssh [계정]@[시스템 주소] -p [포트번호]

cat etc/passwd
-> ssh로 계정에 처음 접속 시 계정을 파악하기위한 용도로 사용해보는 명령어

어떤계정을 쓰고 어떤 권한이 막혀있는지 어떤 레벨까지 있는지 확인할 수 있음

find ./* -> 현재 경로의 모든 파일 찾는 명령어

find [경로] 옵션

-type f -> 타입이 디렉토리인지 파일인지 찾는거임,  f면 파일 d면 디렉토리
-size 1033c -> 바이트 크기가 1033인 파일 찾는 옵션
! -executable -> !은 not -executable은 실행가능한지 보는 옵션

명령어 뒤에 2> /dev/null -> standard error를 쓰레기통으로 보냄명령어 뒤에 2> /dev/null -> standard error를 쓰레기통으로 보냄

파일안에 내용찾는법
1) vi 파일명 -> /내용입력
2) cat data.txt | grep 내용입력 
3) grep 내용입력 파일명 ex) grep millionth ./data.txt

sort 파일명 -> 파일 내용 정렬
uniq -u -> 반복되는 부분 없앰

strings 파일명 -> 사람이 읽을수 있는 부분을 추출하는 명령어

** BASE64 인코딩

암호화 <-> 복호화

인코딩 vs 암호화 (key)

base64 --decode data.txt -> data.txt를 디코딩하는 명령어
끝에 == or = 로 돼있으면 base64로 인코딩돼있을지 의심해볼수있음

----ROT13암호화---
vi 에디터에서 바로 활용하시기 좋게 핵심만 3줄로 요약해 드릴게요.

📝 ROT13 (tr 명령어) 3줄 요약
원리: 알파벳 26자를 13글자씩 두 그룹으로 나눠서 서로의 자리를 1:1로 맞바꾸는 방식입니다.

특징: A ↔ N, a ↔ n처럼 파트너가 정해져 있어, 같은 명령어를 두 번 쓰면 원래 문장으로 돌아옵니다.

작동: tr 기계가 글자를 한 알씩 집어 들고 "암기표"대로 즉시 갈아 끼워 내보내므로 서로 꼬이지 않습니다

tr(바꾸는명령어) [A-Za-z](바꿀대상) [N-ZA-Mn-za-m](바뀐거) 

file

10101010101010101010101010101

-> 이진수를 이렇게 16진수로 바꿈

hexdump file
f1 2f 2d 9d

xxd -> hexdump로 만들거나 hexdump로 된걸 다시 복구시켜주는 명령어
gunzip [gz파일명] -> gzip파일 압축해제시켜주는 명령어 
ex) gunzip bandit.gz
bzip2 -d [bzip2파일명] -> bzip2파일 압축해제시켜주는 명령어
ex) bzip2 -d bandit.bz2
tar -xf [tar파일명] -> tar파일 압축해제시켜주는 명령어
ex) tar -xf bandit.tar

FO5dwFsc0cbaIiH0h8J2eUks2vdTDwAn bandit13번 비번





